name: Deploy to NAS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via Docker Socket
        run: |
          /workspace/training-analyzer/deploy-from-runner.sh
