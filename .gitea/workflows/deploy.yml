name: Deploy to NAS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Execute deployment script
        run: |
          /volume1/docker/training-analyzer/deploy.sh
